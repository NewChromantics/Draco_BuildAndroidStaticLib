Draco_BuildAndroidStaticLib
=====================
- https://github.com/google/draco does not build android static libs
- This repository uses the draco repository has a submodule, and then uses that to build a static lib artifact
